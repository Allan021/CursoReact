import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js';//importando la carpeta de mi aplicacion que es un componente y ademas los import iempre van arriba
const root = document.getElementById("root");


ReactDOM.render(<App />,root)